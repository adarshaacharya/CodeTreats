language: node_js
node_js:
    - 12.18.3

install:
    - npm ci

before_script:
  - npm run develop:ci -- --silent &

# keep the npm cache around to speed up installs
cache:
  directories:
  - "$HOME/.npm"



